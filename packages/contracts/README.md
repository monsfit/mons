# Contracts

This package owns the runtime validation and TypeScript types used by the Effect HTTP API.

The production data pipeline is maintained separately. Public HTTP and catalog read contracts stay
in this repository so clients and alternative catalog implementations can interoperate with Mons.
